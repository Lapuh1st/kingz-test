import React from "react";
import "./search-bar.scss";

let MainPage = () => {

  return (
    <form className="search">
        <input className="search__input" placeholder="Search for songs, artists etc ..."/>
    </form>
  );
};

export default MainPage;